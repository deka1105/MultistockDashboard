[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "session"
addopts = ["-v", "--tb=short"]
markers = [
    "unit: fast, no I/O",
    "integration: requires DB + Redis",
    "e2e: full HTTP stack",
    "slow: takes > 1s",
]
